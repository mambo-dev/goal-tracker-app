import RequestResetForm from "@/components/accounts/forgot-password/request-reset";
import React from "react";

type Props = {};

export default function RequestResetPage({}: Props) {
  return <RequestResetForm />;
}
